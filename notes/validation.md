# 验证记录与工程边界

[返回作品集](../README.md)

本页区分已存在的历史验证与本次作品集整理，不将测试结果扩大为完整平台或发布验收。

## TrainingGround 历史记录

| 时间 | 模式 | 通过 / 总计 | 方法 |
| :--- | :--- | :--- | :--- |
| 2026-09-10 | 编辑模式 | 182 / 182 | 编辑器内直接执行同步 NUnit 断言 |
| 2026-09-10 | 运行模式 | 96 / 96 | Play Mode 内直接执行同步 NUnit 断言 |

覆盖方向包括二段跳、树干几何与接触、技能与刚体响应，以及移动到施法的连续状态切换。相关回归覆盖 0.01、0.02、0.033333 秒等固定步长。

两组用例有重叠，不合并宣称 278 项独立测试；固定步长检查不是对应渲染帧率下的设备性能测量。报告早于后续木棍动作改动，不自动代表最新全部代码已重新通过。

## 本次核对

2026-09-19 根据本地主要实现、配置、场景/Prefab 脚本引用、阶段工作文档及已有报告重新核对作品集，本次补入 LostHeart 的循环等距导航条。未在本次整理中重新运行 Unity 测试或进行真机测量。公开内容不附内部日志、用户路径、账号配置、签名信息或完整工程。

## 展示范围

| 项目 | 本作品集展示 | 尚未作为完成成果展示 |
| :--- | :--- | :--- |
| TrainingGround | 运动、二跳、规则树干攀爬、连击、旋风、触控接入 | 完整养成系统、任意地形攀爬、移动端/微信发布验收 |
| LostHeart | 本地双角色、分屏、提灯联动 AI、UI 框架与工具、循环等距导航条 | 网络联机、主机适配、商业上线 |

循环等距导航条目前只支持**单个目标**，提示箭头已决定不做，方位文本 / 指示器 / 圆弧中心使用缩放前边界，显示角度限制在 `(0, 360]`。它的参数与观感由场景内人工观察验收，**未纳入自动化测试**。

物理查询部分使用 NonAlloc 封装，但系统仍有分配式查询，不宣称全局零 GC。当前没有以统一设备和场景核实的性能提升百分比。

TrainingGround 本地保留 2026-09-11 生成的 Android 开发测试 APK，最终一次构建状态为 `Succeeded`，输出约 96 MB。该记录用于证明 Android 包导出完成；尚未据此宣称目标设备性能、完整真机操作或商店发布通过。

截图用于呈现开发阶段实际效果。模型制作分工经作者确认：TrainingGround 中的模型由另一位团队成员制作，LostHeart 中的模型均由作者本人建模。该说明针对模型，不扩大为全部贴图、音频或其他资源的制作归属。
